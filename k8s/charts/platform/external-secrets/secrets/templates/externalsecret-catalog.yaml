{{- if .Values.services.catalog.enabled }}
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: externalsecret-catalog-service
  namespace: {{ .Values.targetNamespace }}
spec:
  refreshInterval: 1m
  secretStoreRef:
    name: {{ .Values.store.name }}
    kind: ClusterSecretStore
  target:
    name: {{ .Values.services.catalog.targetSecretName }}
    creationPolicy: Owner
  data:
  {{- range $targetKey, $sourceKey := .Values.services.catalog.data }}
    - secretKey: {{ $targetKey }}
      remoteRef:
        key: {{ $.Values.source.secretName }}
        property: {{ $sourceKey }}
  {{- end }}
{{- end }}