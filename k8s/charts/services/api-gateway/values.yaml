replicaCount: 1

image:
  repository: ghcr.io/arni1197/api-gateway
  tag: "latest"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 3001

resources: {}

secrets:
  envSecretName: secret-api-gateway