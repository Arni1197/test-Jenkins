apiVersion: v1
kind: Service
metadata:
  name: audit-service
  labels:
    app: audit-service
spec:
  type: ClusterIP

  selector:
    app: audit-service

  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}