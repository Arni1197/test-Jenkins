apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: staging-new-api
  namespace: kong
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-staging
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - staging-new.90.156.210.86.nip.io
      secretName: staging-new-api-tls
  rules:
    - host: staging-new.90.156.210.86.nip.io
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: kong-kong-proxy
                port:
                  number: 80