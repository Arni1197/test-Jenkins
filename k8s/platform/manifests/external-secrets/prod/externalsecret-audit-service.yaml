apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: secret-audit-service
  namespace: prod
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-backend
  target:
    name: secret-audit-service
    creationPolicy: Owner
    deletionPolicy: Retain
  dataFrom:
    - extract:
        key: kv/app/prod/audit-service