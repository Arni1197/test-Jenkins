apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: secret-api-gateway
  namespace: staging
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-backend
  target:
    name: secret-api-gateway
    creationPolicy: Owner
    deletionPolicy: Retain
  dataFrom:
    - extract:
        key: kv/app/staging/api-gateway