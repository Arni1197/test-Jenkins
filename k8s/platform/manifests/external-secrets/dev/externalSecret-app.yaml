apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: app-db-secret
  namespace: dev

spec:
  refreshInterval: 1h

  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore

  target:
    name: app-db-secret
    creationPolicy: Owner

  data:
    - secretKey: host
      remoteRef:
        key: kv/app/dev/postgres-app
        property: host

    - secretKey: port
      remoteRef:
        key: kv/app/dev/postgres-app
        property: port

    - secretKey: database
      remoteRef:
        key: kv/app/dev/postgres-app
        property: database

    - secretKey: username
      remoteRef:
        key: kv/app/dev/postgres-app
        property: username

    - secretKey: password
      remoteRef:
        key: kv/app/dev/postgres-app
        property: password

    - secretKey: sslmode
      remoteRef:
        key: kv/app/dev/postgres-app
        property: sslmode