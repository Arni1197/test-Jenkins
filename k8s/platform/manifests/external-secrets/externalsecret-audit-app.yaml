apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: secret-audit-service
  namespace: dev
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore

  target:
    name: secret-audit-service
    creationPolicy: Owner
    deletionPolicy: Retain

  dataFrom:
    - extract:
        key: kv/app/dev/audit-service