apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: secret-user-service
  namespace: dev
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore

  target:
    name: secret-user-service
    creationPolicy: Owner
    deletionPolicy: Retain

  dataFrom:
    - extract:
        key: kv/app/dev/user-service