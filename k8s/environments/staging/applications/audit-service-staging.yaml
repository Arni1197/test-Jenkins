apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: audit-service-staging
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Arni1197/test-Jenkins.git
    targetRevision: main
    path: k8s/charts/services/audit-service
    helm:
      valueFiles:
        - ../../../environments/staging/values/services/audit-service.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: staging
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true