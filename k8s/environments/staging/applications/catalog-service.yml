apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: catalog-service-staging
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Arni1197/test-Jenkins.git
    targetRevision: main
    path: k8s/charts/services/catalog-service
    helm:
      valueFiles:
        - ../../../environments/staging/values/services/catalog-service.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: game-services-staging
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true