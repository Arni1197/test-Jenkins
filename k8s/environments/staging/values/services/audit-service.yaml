replicaCount: 1

image:
  repository: ghcr.io/arni1197/audit-service
  tag: sha-REPLACE_ME
  pullPolicy: IfNotPresent

containerPort: 3000

affinity:
  nodeAffinity:
    requiredDuringSchedulingIgnoredDuringExecution:
      nodeSelectorTerms:
        - matchExpressions:
            - key: workload
              operator: In
              values:
                - apps

resources:
  requests:
    cpu: 50m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 256Mi

secretName: secret-audit-service

env:
  NODE_ENV: "staging"