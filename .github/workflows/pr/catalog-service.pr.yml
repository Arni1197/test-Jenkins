name: catalog-service | PR checks

on:
  pull_request:
    branches: ["main"]
    paths:
      - "catalog-service/**"
      - ".github/workflows/pr/catalog-service.pr.yml"
      - ".github/workflows/_lib/node-pr-checks.yml"

permissions:
  contents: read

concurrency:
  group: pr-catalog-service-${{ github.ref }}
  cancel-in-progress: true

jobs:
  checks:
    uses: ./.github/workflows/_lib/node-pr-checks.yml
    with:
      service_name: "catalog-service"
      working_directory: "catalog-service"
      node_version: "20"
      cache_dependency_path: "catalog-service/package-lock.json"
      run_lint: true
      run_test: true
      run_build: false