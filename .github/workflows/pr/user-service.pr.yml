name: user-service | PR checks

on:
  pull_request:
    branches: ["main"]
    paths:
      - "user-service/**"
      - ".github/workflows/pr/user-service.pr.yml"
      - ".github/workflows/_lib/node-pr-checks.yml"

permissions:
  contents: read

concurrency:
  group: pr-user-service-${{ github.ref }}
  cancel-in-progress: true

jobs:
  checks:
    uses: ./.github/workflows/_lib/node-pr-checks.yml
    with:
      service_name: "user-service"
      working_directory: "user-service"
      node_version: "20"
      cache_dependency_path: "user-service/package-lock.json"
      run_lint: true
      run_test: true
      run_build: false