name: frontend | main build & dev deploy (via PR)

on:
  push:
    branches: ["main"]
    paths:
      - "frontend/**"
      - ".github/workflows/main/frontend.main.yml"

permissions:
  contents: write
  packages: write
  pull-requests: write

concurrency:
  group: main-frontend-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build_push:
    name: frontend | Docker build & push
    uses: ./.github/workflows/_lib-docker-build-push.yml
    permissions:
      contents: read
      packages: write
    with:
      service_name: "frontend"
      context: "frontend"
      dockerfile: "frontend/Dockerfile"
      image: "ghcr.io/arni1197/frontend"
      tag: "sha-${{ github.sha }}"
      push: true
      platforms: "linux/amd64"

  update_dev_values_pr:
    name: frontend | PR update dev values
    needs: [build_push]
    uses: ./.github/workflows/_lib-promote-pr.yml
    permissions:
      contents: write
      pull-requests: write
    with:
      service_name: "frontend"
      target_env: "dev"
      target_values_file: "k8s/environments/local/values/services/frontend.yaml"
      new_tag: "sha-${{ github.sha }}"
      new_digest: "${{ needs.build_push.outputs.digest }}"
      base_branch: "main"
      pr_branch_prefix: "promote/dev"
      pr_labels: "gitops,dev"
      skip_if_exists: true